/**
 * 
 * @author Dmitriy
 * @module
 */ 
function importSettings(filePath, impVariant) {
    var self = this, model = this.model;
    
    
  /*
   * функция создает объект с типом сопоставления для отправки его на импорт
   * impVariant - вариант импорта 
   */
    function makeObj4Import(impVariant) {
        
    }
    
   /*
    * функция создает объект импорт
    * filefilePath - путь файла 
    * obj4Import - объект с сопоставлением
    */
    function createImpObj(filePath, obj4Import) {
        
    }
    
    /*
     * функция добавляет настройки к объекту для импорта
     * obj4Import - объект с типом сопоставления для отправки в импорт
     * number - номер ячейки 
     * mapping - тим сопоствления 
     * isArray = если массив
     */
    function addSetting(number, mapping, isArray) {
        
    }
    
    /*
     * функция удаляет тип сопоставления
     * obj4Import - объект с типом сопоставления для отправки в импорт
     * number - номер ячейки
     */
    function deleteMapping(number) {
        
    }
    
    function setSetting(number, mapping, isArray) {
        
    }
    
    /*
     * функция проверяет obj4Import и если есть пустой тип сопоставления переписывает obj4Import
     * obj4Import - объект с типом сопоставления для отправки в импорт
     */
    function checkImportObject(obj4Import) {
        
    }
}
